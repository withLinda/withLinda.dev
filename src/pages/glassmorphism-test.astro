---
import BaseHead from '../components/BaseHead.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import Body from '../components/Body.astro'
import Content from '../components/Content.astro'
import { SITE_TITLE, SITE_DESCRIPTION } from '../config'
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <BaseHead title="Glassmorphism Test" description="Testing glassmorphism effects" />
  </head>
  <Body>
    <Header />
    <Content>
      <style>
        @reference "../styles/global.css";
        
        .test-card {
          @apply relative overflow-hidden rounded-2xl border border-gray-200 dark:border-gray-700;
          @apply bg-white/80 dark:bg-gray-900/80;
          @apply shadow-lg hover:shadow-2xl;
          @apply transition-all duration-300 ease-out;
          @apply hover:-translate-y-1 hover:scale-[1.02];
          @apply p-6 mb-6;
        }
        
        @supports (backdrop-filter: blur(12px)) or (-webkit-backdrop-filter: blur(12px)) {
          .test-card {
            @apply bg-gradient-to-br from-white/10 to-white/5 dark:from-white/5 dark:to-white/[0.02];
            @apply border-white/10 dark:border-white/20;
            backdrop-filter: blur(12px) saturate(1.5);
            -webkit-backdrop-filter: blur(12px) saturate(1.5);
          }
        }
        
        .test-card::before {
          content: '';
          @apply absolute inset-0 bg-gradient-to-br from-white/20 to-transparent opacity-0;
          @apply transition-opacity duration-300;
          pointer-events: none;
        }
        
        .test-card:hover::before {
          @apply opacity-100;
        }
        
        .test-card:hover {
          @apply border-white/30 dark:border-white/40;
          transform: translateY(-4px) scale(1.02) translateZ(0);
        }
        
        .colorful-bg {
          background: linear-gradient(45deg, 
            #ff6b6b 0%, 
            #4ecdc4 25%, 
            #45b7d1 50%, 
            #f9d56e 75%, 
            #6c5ce7 100%);
          background-size: 400% 400%;
          animation: gradient 15s ease infinite;
          height: 200px;
          border-radius: 1rem;
          margin-bottom: 2rem;
        }
        
        @keyframes gradient {
          0% { background-position: 0% 50%; }
          50% { background-position: 100% 50%; }
          100% { background-position: 0% 50%; }
        }
      </style>

      <h1 class="text-3xl font-bold mb-8 text-solarized-fg dark:text-nightowl-fg">
        Glassmorphism Test Page
      </h1>
      
      <p class="mb-6 text-solarized-text dark:text-nightowl-text">
        This page tests if glassmorphism effects are working properly. In dark mode, you should see animated orbs in the background and glassmorphic cards with blur effects.
      </p>
      
      <!-- Colorful background to test blur effect -->
      <div class="colorful-bg"></div>
      
      <div class="test-card">
        <h2 class="text-xl font-semibold mb-3 text-solarized-blue dark:text-nightowl-blue">
          Test Card 1
        </h2>
        <p class="text-solarized-text dark:text-nightowl-text">
          This card should have a glassmorphic effect with backdrop blur. In dark mode, you should see the animated orbs blurred behind this card.
        </p>
      </div>
      
      <div class="test-card">
        <h2 class="text-xl font-semibold mb-3 text-solarized-blue dark:text-nightowl-blue">
          Test Card 2
        </h2>
        <p class="text-solarized-text dark:text-nightowl-text">
          Hover over this card to see the illumination effect. The card should lift up and show a subtle glow overlay.
        </p>
      </div>
      
      <div class="test-card">
        <h2 class="text-xl font-semibold mb-3 text-solarized-blue dark:text-nightowl-blue">
          Test Card 3
        </h2>
        <p class="text-solarized-text dark:text-nightowl-text">
          The background content (animated gradient above and orbs in dark mode) should be blurred behind these glassmorphic cards.
        </p>
      </div>
      
      <div class="mt-8 p-4 bg-solarized-secondary dark:bg-nightowl-secondary rounded-lg">
        <h3 class="font-semibold mb-2 text-solarized-fg dark:text-nightowl-fg">Expected Behavior:</h3>
        <ul class="text-sm text-solarized-text dark:text-nightowl-text space-y-1">
          <li>• Light mode: Cards should be semi-transparent with paper texture background</li>
          <li>• Dark mode: Cards should be glassmorphic with blurred animated orbs behind them</li>
          <li>• Hover: Cards should lift up with glow overlay and scale slightly</li>
          <li>• Browser support: Should gracefully fallback on unsupported browsers</li>
        </ul>
      </div>
      
    </Content>
    <Footer />
  </Body>
</html>